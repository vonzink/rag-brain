{
  "name": "msfg-rag-s3-ingest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sync the S3 rag-brain/ corpus into the Mortgage Brain via its admin upload API.",
  "scripts": {
    "test": "node --test",
    "sync": "node sync.mjs",
    "dry-run": "node sync.mjs --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
